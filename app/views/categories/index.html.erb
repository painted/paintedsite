<% if @categories.any? %>
	<% @categories.each do |category| %>
		<%= image_tag category.image.url(:thumb), class: 'uploaded-pic' %><br>
		<%= category.title %>
		<%= category.description %>
	<% end %>
<% else %>
	No categories yet
<% end %>